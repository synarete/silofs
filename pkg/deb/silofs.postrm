#!/bin/sh
set -e

#DEBHELPER#

if [ "$1" = "upgrade" ]; then
    if command -v systemctl >/dev/null 2>&1; then
        systemctl daemon-reload || true
        systemctl try-restart silofs-mountd.service || true
    fi
fi
