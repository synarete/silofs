# Config file for silofs-mountd service.
#
# Lines should have one of the the following formats:
#
#  mntpath1
#  mntpath2 uid=UID
#  ...
#
# Where:
#  mntpath   -- Allow silofs.fuse mounts on this directory path
#  uid=UID   -- Only user with UID may mount/umount this path (optional)
#
